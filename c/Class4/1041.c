#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main1041(int argc, char *argv[]) {
	char strA[80];
	char strB[80];
	char strC[80];
	scanf("%s ", strA);
	gets(strB);
	gets(strC);
		
	printf("%s\n", strA);
	printf("%s\n", strB);
	puts(strC);
	return 0;
}
