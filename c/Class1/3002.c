#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
//±è§Î­±¿n 
int main3002(int argc, char *argv[]) {
	double num1, num2, num3;
	scanf("%lf%lf%lf", &num1, &num2, &num3);
	printf("%.1lf", (num1+num2)*num3/2);
	return 0;
}
